# ─────────────────────────────────────────────────────────────────────────────
# docker/requirements.inference.txt
#
# [IMMEDIATE FIX 3] Inference-service-only dependencies.
#
# These packages are REMOVED from the main requirements.txt:
#   - torch==2.3.0        (~2.3 GB CPU / much more with CUDA)
#   - transformers==4.40.0
#   - huggingface-hub==0.23.2
#
# The main API calls this service over HTTP; it does NOT import torch directly.
# ─────────────────────────────────────────────────────────────────────────────

# ── Inference runtime ──────────────────────────────────────────────────────────
torch==2.3.0
transformers==4.40.0
huggingface-hub==0.23.2
sentencepiece==0.2.0        # required by some tokenizers (flan-t5, mT5)
accelerate==0.30.1          # device_map="auto" support
safetensors==0.4.3          # fast / safe model loading

# ── Science stack (shared with main app but pinned independently here) ────────
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.0
joblib==1.4.2

# ── Inference HTTP server ─────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
httpx==0.27.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
structlog==24.2.0
tenacity==8.3.0
