# ─────────────────────────────────────────────────────────────────────────────
# EduBoost SA — Main API Python Dependencies
#
# [IMMEDIATE FIX 3] torch, transformers, huggingface-hub REMOVED from here.
#                   They now live in docker/requirements.inference.txt and are
#                   only installed in the dedicated inference service container.
#
# [SHORT-TERM FIX 6] supabase client removed — Postgres is self-managed via
#                    asyncpg + SQLAlchemy. See docs/architecture.md.
#
# [SHORT-TERM FIX 6] anthropic updated from 0.28.0 → latest stable.
#
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── WEB FRAMEWORK ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── DATABASE ──────────────────────────────────────────────────────────────────
# supabase==2.4.6  ← REMOVED: using self-managed Postgres via asyncpg
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9      # used by Alembic sync migrations only

# ── AUTHENTICATION & SECURITY ────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.8
pydantic[email]==2.7.1
pydantic-settings==2.2.1
slowapi==0.1.9               # API rate limiting

# ── AI / LLM CLIENTS ─────────────────────────────────────────────────────────
# [FIX] anthropic updated from 0.28.0 to latest stable (Claude 4 model support)
anthropic==0.40.0
groq==0.9.0
openai==1.30.5               # Groq's OpenAI-compatible API

# NOTE: torch, transformers, huggingface-hub → see docker/requirements.inference.txt

# ── ML / ADAPTIVE ENGINE (lightweight — no torch) ────────────────────────────
scikit-learn==1.5.0
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
joblib==1.4.2                # Model serialisation

# ── CACHING ───────────────────────────────────────────────────────────────────
redis==5.0.4
hiredis==2.3.2

# ── BACKGROUND TASKS ──────────────────────────────────────────────────────────
celery==5.4.0
flower==2.0.1                # Celery monitoring

# ── EMAIL ─────────────────────────────────────────────────────────────────────
sendgrid==6.11.0
jinja2==3.1.4

# ── STORAGE ───────────────────────────────────────────────────────────────────
boto3==1.34.110              # Cloudflare R2 (S3-compatible)

# ── MONITORING & LOGGING ─────────────────────────────────────────────────────
prometheus-fastapi-instrumentator==6.1.0
sentry-sdk[fastapi]==2.3.1
structlog==24.2.0

# ── SECRETS MANAGEMENT (Medium-Term) ─────────────────────────────────────────
azure-keyvault-secrets==4.8.0
azure-identity==1.16.0

# ── UTILITIES ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.27.0                # HTTP client / async test client + inference service calls
tenacity==8.3.0              # Retry logic for LLM calls
bleach==6.1.0                # PII scrubbing / input sanitisation
phonenumbers==8.13.37        # SA phone number PII detection
python-dateutil==2.9.0
babel==2.15.0                # i18n/l10n support (Long-Term multilingual feature)

# ── TESTING ───────────────────────────────────────────────────────────────────
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-mock==3.14.0
factory-boy==3.3.0
faker==25.2.0
playwright==1.44.0           # E2E testing (Short-Term)
pytest-playwright==0.5.0
