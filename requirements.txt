# ─────────────────────────────────────────────────────────────────────────────
# EduBoost SA — Python Dependencies
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── WEB FRAMEWORK ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── DATABASE ──────────────────────────────────────────────────────────────────
supabase==2.4.6
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# ── AUTHENTICATION & SECURITY ────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.8
pydantic[email]==2.7.1
pydantic-settings==2.2.1
slowapi==0.1.9                  # Rate limiting

# ── AI / LLM CLIENTS ─────────────────────────────────────────────────────────
anthropic==0.28.0
groq==0.9.0
openai==1.30.5                   # Compatible with Groq's OpenAI-compatible API
huggingface-hub==0.23.2
transformers==4.40.0             # Offline inference support
torch==2.3.0                     # Offline inference support

# ── ML / ADAPTIVE ENGINE ─────────────────────────────────────────────────────
scikit-learn==1.5.0
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
joblib==1.4.2                    # Model serialisation

# ── CACHING ───────────────────────────────────────────────────────────────────
redis==5.0.4
hiredis==2.3.2

# ── BACKGROUND TASKS ──────────────────────────────────────────────────────────
celery==5.4.0
flower==2.0.1                    # Celery monitoring

# ── EMAIL ─────────────────────────────────────────────────────────────────────
sendgrid==6.11.0
jinja2==3.1.4                    # Email templates

# ── STORAGE ───────────────────────────────────────────────────────────────────
boto3==1.34.110                  # Cloudflare R2 (S3-compatible)

# ── MONITORING & LOGGING ─────────────────────────────────────────────────────
prometheus-fastapi-instrumentator==6.1.0
sentry-sdk[fastapi]==2.3.1
structlog==24.2.0

# ── UTILITIES ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.27.0                    # HTTP client / async test client
tenacity==8.3.0                  # Retry logic for LLM calls
bleach==6.1.0                    # PII scrubbing / input sanitisation
phonenumbers==8.13.37            # SA phone number PII detection
python-dateutil==2.9.0

# ── TESTING ───────────────────────────────────────────────────────────────────
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-mock==3.14.0
factory-boy==3.3.0               # Test data factories
faker==25.2.0
# EduBoost SA — Additional requirements for five-pillar architecture
# Append these to your existing requirements.txt

# Pillar 1 — Legislature
sentence-transformers>=2.7.0
openai>=1.30.0

# Pillar 2 — Executive
pydantic>=2.7.0
httpx>=0.27.0

# Pillar 3 — Judiciary
anthropic>=0.28.0

# Pillar 4 — Fourth Estate
redis[hiredis]>=5.0.0
prometheus-client>=0.20.0

# Pillar 5 — Ether
celery>=5.4.0
celery[redis]>=5.4.0

# Infrastructure
pybreaker>=1.0.1
joblib>=1.4.0
numpy>=1.26.0
boto3>=1.34.0

# IRT
scipy>=1.13.0

# POPIA
bleach>=6.1.0
phonenumbers>=8.13.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-timeout>=2.3.0
pact-python>=2.2.0
locust>=2.28.0

# DB
alembic>=1.13.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pgvector>=0.3.0
# EduBoost SA — Additional requirements for five-pillar architecture
# Append these to your existing requirements.txt

# Pillar 1 — Legislature
sentence-transformers>=2.7.0
openai>=1.30.0

# Pillar 2 — Executive
pydantic>=2.7.0
httpx>=0.27.0

# Pillar 3 — Judiciary
anthropic>=0.28.0

# Pillar 4 — Fourth Estate
redis[hiredis]>=5.0.0
prometheus-client>=0.20.0

# Pillar 5 — Ether
celery>=5.4.0
celery[redis]>=5.4.0

# Infrastructure
pybreaker>=1.0.1
joblib>=1.4.0
numpy>=1.26.0
boto3>=1.34.0

# IRT
scipy>=1.13.0

# POPIA
bleach>=6.1.0
phonenumbers>=8.13.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-timeout>=2.3.0
pact-python>=2.2.0
locust>=2.28.0

# DB
alembic>=1.13.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pgvector>=0.3.0
