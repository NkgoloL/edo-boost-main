# Optional local/offline inference dependencies.
# Install only when enabling the HuggingFace fallback:
#   pip install -r requirements-ml.txt

sentence-transformers==2.7.0
transformers==4.40.0
torch==2.3.0
